apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: testingress
  annotations:
   nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  rules:
  - host: api.server.com
    http:
      paths:
        - pathType: Prefix
          path: "/"
          backend:
            service:
              name: api
              port:
                number: 5500
